# BengaluruTrafficAI — Python dependencies
# Install with: pip install -r requirements.txt

# Core CV & detection
ultralytics>=8.3.0          # YOLO11 support (requires ultralytics 8.3+)
opencv-python>=4.8.0        # video processing & image ops
torch>=2.0.0                # PyTorch backend
torchvision>=0.15.0

# Tracking
lap>=0.4.0                  # Linear Assignment Problem solver for ByteTrack

# Pose estimation (helmet/seatbelt)
mediapipe>=0.10.0

# ALPR / OCR
paddleocr>=2.7.0
paddlepaddle>=2.5.0         # CPU build; use paddlepaddle-gpu for CUDA

# Video streaming
yt-dlp>=2024.0.0            # YouTube & online video stream extraction

# API backend (Component 5 - FastAPI)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
pydantic>=2.0.0

# Database - PostgreSQL
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9      # PostgreSQL adapter

# Task queue
celery>=5.3.0
redis>=5.0.0

# Experiment tracking
wandb>=0.16.0               # Weights & Biases

# Dataset management
roboflow>=1.1.0             # Roboflow integration

# Utilities
numpy>=1.24.0
pillow>=10.0.0
python-multipart>=0.0.9     # file uploads in FastAPI
python-dotenv>=1.0.0
aiofiles>=23.0.0
